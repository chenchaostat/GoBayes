% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gobayes_oc.R
\name{gobayes_oc}
\alias{gobayes_oc}
\title{Bayesian Go/No-Go Operating Characteristic Table}
\usage{
gobayes_oc(
  n_total = 100,
  alloc_ratio = c(1, 1),
  delta = 0.05,
  go_delta = NULL,
  nogo_delta = NULL,
  go_prob = 0.7,
  nogo_prob = 0.3,
  prior_t = c(1, 1),
  prior_c = c(1, 1),
  control_rates = 0.3,
  treatment_rates = c(0.3, 0.35, 0.4),
  n_sim = 1e+05,
  mc_seed = 42,
  run_exact = TRUE,
  run_mc = TRUE,
  method5_pc_hat_boundary = NULL,
  integration_tol = 1e-10,
  digits = 1
)
}
\arguments{
\item{n_total}{Integer. Total sample size.}

\item{alloc_ratio}{Numeric vector of length 2. Treatment/control allocation ratio.}

\item{delta}{Numeric. Default treatment-control difference threshold.}

\item{go_delta}{Numeric or NULL. Go threshold delta. If NULL, uses \code{delta}.}

\item{nogo_delta}{Numeric or NULL. No-Go threshold delta. If NULL, uses \code{delta}.}

\item{go_prob}{Numeric. Posterior probability cutoff for Go.}

\item{nogo_prob}{Numeric. Posterior probability cutoff for No-Go.}

\item{prior_t}{Numeric vector of length 2. Beta prior for treatment arm.}

\item{prior_c}{Numeric vector of length 2. Beta prior for control arm.}

\item{control_rates}{Numeric vector. True control response rates.}

\item{treatment_rates}{Numeric vector. True treatment response rates.}

\item{n_sim}{Integer. Number of Monte Carlo simulations.}

\item{mc_seed}{Integer. Monte Carlo random seed.}

\item{run_exact}{Logical. Whether to run exact enumeration.}

\item{run_mc}{Logical. Whether to run Monte Carlo simulation.}

\item{method5_pc_hat_boundary}{Numeric or NULL. Control rate used to derive method 5 boundary.}

\item{integration_tol}{Numeric. Integration tolerance.}

\item{digits}{Integer. Digits used in printing.}
}
\value{
An object of class \code{GoBayesOC}.
}
\description{
Compute Bayesian Go/No-Go operating characteristics by calling a remote
GoBayes API server.
}
\examples{
\dontrun{
gobayes_set_server("http://116.62.190.134:10002")

res <- gobayes_oc(
  n_total = 75,
  alloc_ratio = c(2, 1),
  go_delta = 0.15,
  nogo_delta = 0.25,
  go_prob = 0.80,
  nogo_prob = 0.10,
  control_rates = 0.15,
  treatment_rates = c(0.15, 0.25, 0.35, 0.40, 0.45),
  n_sim = 10000,
  method5_pc_hat_boundary = 0.15
)

print(res)
}
}
